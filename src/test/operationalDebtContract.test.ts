import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("supabase/migrations/20261219145000_develop_operational_debt.sql","utf8").toLowerCase();const service=readFileSync("src/services/operationalDebtService.ts","utf8");const panel=readFileSync("src/components/develop/OperationalDebtPanel.tsx","utf8");const page=readFileSync("src/pages/DevelopmentCaseWorkspacePage.tsx","utf8");
describe("D8.03 operational debt",()=>{it("covers exactly the nine specification classes",()=>{for(const k of ["punch_items","temporary_repairs","workarounds","missing_spares","missing_pms","documentation_gaps","training_gaps","software_gaps","reliability_defects"])expect(sql).toContain(`'${k}'`);expect(sql).toContain("'gapclasses',9");});it("is a register of canonical references, not copied facts",()=>{for(const t of ["acceptance_tests","temporary_modifications","asset_onboarding_items","quality_defects"])expect(sql).toContain(`'${t}'`);expect(sql).not.toContain("source_description");expect(panel).toContain("does not copy the source facts");});it("requires owner and separate operations acknowledgement",()=>{expect(sql).toContain("owner_id uuid not null");expect(sql).toContain("the recorder cannot provide operations acknowledgement");expect(sql).toContain("audit_events");});it("has reachable customer actions",()=>{expect(service).toContain('"record_operational_debt_reference"');expect(service).toContain('"acknowledge_operational_debt"');expect(page).toContain("<OperationalDebtPanel");});});
