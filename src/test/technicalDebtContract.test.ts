import {readFileSync} from "node:fs";import{describe,expect,it}from"vitest";
const sql=readFileSync("supabase/migrations/20261219144000_develop_technical_debt.sql","utf8").toLowerCase();const service=readFileSync("src/services/technicalDebtService.ts","utf8");const panel=readFileSync("src/components/develop/TechnicalDebtPanel.tsx","utf8");const page=readFileSync("src/pages/DevelopmentCaseWorkspacePage.tsx","utf8");
describe("D8.02 technical debt",()=>{it("extends the canonical object only",()=>{expect(sql).toContain("alter table public.temporary_modifications");expect(sql).not.toMatch(/create table/);expect(sql).toContain("deferred_redesign");expect(sql).toContain("incomplete_monitoring");});it("binds debt to case, risk, removal, and optional stated cost",()=>{for(const x of ["origin_development_case_id","required_removal_by","risk_assessment_ref","lifecycle_cost_basis"])expect(sql).toContain(x);expect(sql).toContain("all left unknown");expect(panel).toContain("unvalued—not zero");});it("requires a distinct authorized human approval without invalidating legacy modifications",()=>{expect(sql).toContain("the recorder cannot approve");expect(sql).toContain("awaiting_independent_approval");expect(sql).toContain("origin_development_case_id is null");expect(sql).toContain("audit_events");});it("is reachable on the case workspace",()=>{expect(service).toContain('"record_case_technical_debt"');expect(service).toContain('"approve_case_technical_debt"');expect(page).toContain("<TechnicalDebtPanel");});});
