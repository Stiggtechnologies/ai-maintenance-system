/**
 * Signed-in entry after evaluation signup (/start).
 *
 * The checklist-only Start here floor is superseded by the ask-first
 * Decision Case (acceptance bar 2026-09-06). Intent is not a permission role.
 * Example prompts are not written into the customer workspace.
 */
import { InvertedOpeningPage } from "./InvertedOpeningPage";

export function StartHerePage() {
  return <InvertedOpeningPage />;
}
